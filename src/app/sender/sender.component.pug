main.sender
  div.sender__title Заполнить оставшиеся поля
  div.sender__recip-number Количество получателей:
  form
    input.sender__letter-name(type="text")
    input.sender__email-subject(type="text")
    input.sender__from-name(type="text")
    input.sender__from-address(type="email")
    input.sender__send-buttom(type="submit")

